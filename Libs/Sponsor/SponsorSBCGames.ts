namespace Sponsor {

    export class SponsorSBCGames extends Sponsor {

        // --------------------------------------------------------------------
        public constructor(features: ISponsorFeatures) {
            super(features);
        }
    }
}
